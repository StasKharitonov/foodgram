volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: always

  backend:
    image: theacidonly/foodgram_backend:latest
    env_file: .env
    volumes:
      - static:/backend_static
      - ./data:/app/data
      - media:/app/media
    depends_on:
      - db
    restart: always

  frontend:
    image: theacidonly/foodgram_frontend:latest
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static:/frontend_static
    restart: "no"

  gateway:
    image: theacidonly/foodgram_gateway:latest
    env_file: .env
    volumes:
      - static:/staticfiles/
      - media:/media
    ports:
      - 7000:80
    depends_on:
      - backend
      - frontend
    restart: always
